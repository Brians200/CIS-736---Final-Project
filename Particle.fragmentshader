#version 330 core

uniform sampler2D mytexture;
in vec3 fragmentColor;

// Ouput data
//out vec3 color;

void main()
{
	// Output color = red 
	//color = fragmentColor;
	gl_FragColor = texture2D(mytexture, gl_PointCoord) * vec4(fragmentColor,1);

}